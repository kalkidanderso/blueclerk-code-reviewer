model Invoiceprefix {
    id            Int    @id @default(autoincrement())
    alternativeId String @default(cuid())
    prefix        String
    maxInvoiceId  Int
    company       Int
    // TODO: COMPANY RELATIONSHIP
}
